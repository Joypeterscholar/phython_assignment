# def compute_tax)(status: str, taxable_income: float):
# after_tax = 0
# if taxable_income < 0
# return "tax income cannot be negative"
# if taxable_income < 50_000:
#     return f"your income $ {taxable_income:,.2f} is not taxable"
# match status.lower():
#     case "single":
#         if taxable_income ==